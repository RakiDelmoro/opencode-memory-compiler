[project]
name = "opencode-memory-compiler"
version = "0.1.0"
description = "Extract knowledge from OpenCode conversations into a structured wiki"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
]
